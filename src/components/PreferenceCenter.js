import React, { Component } from 'react';

class PreferenceCenter extends Component {
  render () {
    // todo: move list component into preference center
    // create two lists with sortable components between
    
  }
}
